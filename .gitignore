node_modules/
dist/
*.log
.vite/
coverage/

# android/ is committed on purpose (Capacitor's native project; its own .gitignore drops build output).
# The web copy inside it is regenerated by `npx cap sync`.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
__pycache__/
