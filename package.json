{
  "name": "tamagotchia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A pocket creature that lives on your phone. A deterministic engine keeps the truth; a language model only gives it a voice.",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
